---
description: "Set up chezmoi (use /chezmoi:chezmoi-setup instead)"
---

This command has been replaced by the `chezmoi-setup` skill.

Use `/chezmoi:chezmoi-setup` or say "set up chezmoi".

The skill provides the same functionality with Progressive Disclosure for better context management.
