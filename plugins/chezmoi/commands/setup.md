---
description: "Set up chezmoi (use /chezmoi:chezmoi-setup skill instead)"
---

This command has been replaced by the `chezmoi-setup` skill.

Use `/chezmoi:chezmoi-setup` or say "set up chezmoi".

The skill provides the same functionality and loads details on demand to reduce context usage.
