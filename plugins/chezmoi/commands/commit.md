---
description: "Commit dotfiles (use /chezmoi:chezmoi-commit skill instead)"
---

This command has been replaced by the `chezmoi-commit` skill.

Use `/chezmoi:chezmoi-commit` or say "commit dotfiles".

The skill provides the same functionality and loads details on demand to reduce context usage.
