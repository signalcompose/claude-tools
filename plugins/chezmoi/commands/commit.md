---
description: "Commit dotfiles (use /chezmoi:chezmoi-commit instead)"
---

This command has been replaced by the `chezmoi-commit` skill.

Use `/chezmoi:chezmoi-commit` or say "commit dotfiles".

The skill provides the same functionality with Progressive Disclosure for better context management.
