---
description: "Check dotfiles status and sync state with remote"
---

# Chezmoi Status Check

Execute the following Bash script to check dotfiles status:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/scripts/chezmoi-check.sh
```

After execution, report the results to the user.
